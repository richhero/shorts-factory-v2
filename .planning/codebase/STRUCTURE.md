# STRUCTURE.md

## Directory Map
- `src/`: Core logic and components.
  - `App.jsx`: Main entry and layout orchestrator.
  - `App.css`: Global tokens (to be modularized).
- `tools/gsd/`: GSD workflow engine (installed).
- `.planning/`: GSD persistent memory and context engineering.

## Future Refactoring Target (GSD Phase 1)
- `src/styles/`:
  - `Layout.css` (Sidebar, Header)
  - `Editor.css` (Preview, Tool Panel)
  - `Modal.css` (Export UI)
  - `Timeline.css` (Timeline Canvas)
